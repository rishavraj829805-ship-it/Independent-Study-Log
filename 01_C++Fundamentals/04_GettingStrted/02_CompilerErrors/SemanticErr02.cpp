#include <iostream>

using namespace std;

int main(){
    
    // Here the dividing is allowed in c++ but we tried to divide a string value to an integer value which is not valid and throws a semantic error
    cout << ("Hello World" / 125) << endl;
    return 0;
}